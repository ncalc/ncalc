# NCalcAsync

NCalc is a mathematical expression evaluator for .NET.
`NCalcAsync` is obsolete and only redirects to `NCalc.Core`.

Use `NCalc.Core` directly for the shared expression engine and async evaluation API.

For more information, [read the docs](https://ncalc.github.io/ncalc/articles/async.html).
